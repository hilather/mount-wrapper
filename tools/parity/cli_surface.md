# CLI surface inventory

Generated by `tools/parity/cli_surface.sh`.

- **mount-wrapper root:** repo root (script: `tools/parity/cli_surface.sh`)
- **Upstream:** compared against sibling `../tarmount-wsl` (or `TARMOUNT_WSL_ROOT`)
- **Date (UTC):** 2026-08-01T02:37:26Z

## mount-wrapper commands

| Command | Notes |
|---------|-------|
| `version` | offline |
| `help` | offline |
| `serve` | daemon; `--config` `--once` `--allow-unauth` |
| `doctor` | offline; `--json` `--fix-systemd` |
| `config show` | local or via serve for apply |
| `config set` | local or via serve for apply |
| `status` | requires running serve (control socket) |
| `metrics` | requires running serve (control socket) |
| `rescan` | requires running serve (control socket) |
| `retry` | requires running serve (control socket) |
| `mount` | requires running serve (control socket) |
| `unmount` | requires running serve (control socket) |
| `purge` | requires running serve (control socket) |
| `hooks list` | requires running serve (control socket) |
| `hooks status` | requires running serve (control socket) |

### Intentional differences vs upstream CLI

| Topic | tarmount-wsl | mount-wrapper |
|-------|--------------|---------------|
| Binary name | `tarmount-wsl` | `mount-wrapper` only (D2) |
| `web` command | Sidecar `tarmount-wsl web` | **Embedded** in `serve` when `web_enabled` (D4); no separate web CLI |
| serve foreground | `--foreground` flag | Process is always foreground |
| Hooks env in docs | `TARMOUNT_*` | `MOUNT_WRAPPER_*` only (D3) |

## Upstream commands (parsed)

| Command |
|---------|
| `config show` |
| `config set` |
| `hooks list` |
| `hooks status` |
| `version` |
| `serve` |
| `doctor` |
| `status` |
| `metrics` |
| `rescan` |
| `retry` |
| `web` |
| `mount` |
| `unmount` |
| `purge` |

## Diff (set-level, normalized)

### In upstream only

- `web`

### In mount-wrapper only

- `help`

## Live binary help excerpt

```
mount-wrapper — archive auto-mounter orchestrator

Usage:
  mount-wrapper <command> [flags]

Commands:
  version              Print version
  help                 Show this help
  serve                Run the daemon (scan / mount / hooks loop)
  doctor               Offline environment diagnostics
  config show|set      Show or update configuration
  status               Service status (requires serve)
  metrics [ARCHIVE_ID] Archive size metrics (requires serve)
  rescan               Trigger an immediate source scan (requires serve)
  retry ARCHIVE_ID     Reset mount attempts (requires serve)
  mount PATH           Request mount of an archive path (requires serve)
  unmount TARGET|--all Unmount managed archive(s) (requires serve)
  purge ARCHIVE_ID     Purge state/index/overlay (requires --yes; serve)
  hooks list|status    Inspect first-mount hooks (requires serve)

Common flags:
  --config, -c PATH    Config YAML (default: /etc/mount-wrapper/config.yaml)
  --socket PATH        Control socket (overrides config control_socket)

serve flags:
  --once               Single tick then exit (tests / oneshot debug)
  --allow-unauth       Allow unauthenticated control (env also supported)

doctor flags:
  --json               JSON report
  --fix-systemd        Write systemd drop-in for source paths

config show flags:
  --local              Read config file only (no control socket)

config set flags:
  --json JSON | --file PATH   Patch or full config
  --patch              Treat root object as shallow patch
  --dry-run            Validate only; do not write

```
